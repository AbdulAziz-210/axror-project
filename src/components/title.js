
const Title = (props) => {
    return (
        <div>
            <h1 className="title">{props.title}</h1>
            <br />
            <p className="subtitle">{props.subtitle}</p>
        </div>
    )
    
}

export default Title